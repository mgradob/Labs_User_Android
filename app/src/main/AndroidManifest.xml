<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itesm.labs.labsuser" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/LabsTheme" >
        <activity
            android:name=".activities.activities.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.activities.SignUpActivity"
            android:label="@string/title_activity_sugn_up" >
        </activity>
        <activity
            android:name=".activities.activities.LaboratoriosActivity"
            android:label="@string/title_activity_laboratorios"
            android:parentActivityName=".activities.activities.LoginActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.activities.LoginActivity" />
        </activity>
        <activity
            android:name=".activities.activities.CategoriesActivity"
            android:label="@string/title_activity_categories"
            android:parentActivityName=".activities.activities.LaboratoriosActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.itesm.labs.labsuser.activities.activities.LaboratoriosActivity" />
        </activity>
    </application>

</manifest>
